sudo: false
language: python
python:
    - "3.2"
    - "3.3"
    - "3.4"
    - "3.5"
    - "3.6"
addons:
    apt:
        packages:
            - liblapack-dev
            - libatlas-dev
            - gfortran
install: 
    - pip install -r requirements.txt
    - python setup.py install
script: nosetests -v
